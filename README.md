# QA Playwright Project

This project contains automated UI tests using Playwright with Docker support.

---

## 🔧 Tech Stack
- Playwright
- TypeScript
- Docker
- Docker Compose

---

## 📦 Install Dependencies (Local)

```bash
npm install

▶️ Run Tests Locally
npx playwright test

🐳 Run Tests with Docker
cd docker
docker compose up --build

✅ Test Cases

Login - happy path

Add item to cart and verify

📊 Parallel Execution

Tests are executed using multiple workers for faster execution.


กด Save ✔

---

# ✅ STEP 2 — เช็คสถานะ git

ใน Terminal ที่ root project พิมพ์:

```bash
git status